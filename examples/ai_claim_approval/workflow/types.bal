type SubmitReview record {
    string comment;
    string result;
};

// type Claim record {|
//     string claim;
//     string amount;
// |};

type ReviewDetails record {
    string reason;
    string claims;
};